language: python
python:
  - "3.8"
  - "3.7"
  - "3.6"

env:
  - INSTALL=travis/linter-setup.sh
    SCRIPT=travis/linter-run.sh

  - INSTALL=travis/tox-run.sh
    SCRIPT=travis/tox-setup.sh

services:
  - mysql

before_script:
  - bash $INSTALL

script:
  - bash $SCRIPT
